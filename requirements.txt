# Beyond Images pipeline dependencies (tested with Python 3.13).
#
# PyTorch is intentionally NOT listed: install it first with the CUDA index
# matching your GPU (see scripts/setup_env.sh), e.g.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu132
# CPU-only: pip install torch torchvision

numpy>=2.4.4
pyyaml>=6.0.3
tqdm>=4.68.4
requests>=2.34.2
beautifulsoup4>=4.15.0
pillow>=12.2.0
transformers>=5.14.1
accelerate>=1.14.0
sentence-transformers>=5.6.0
h5py>=3.16.0

# Interactive Image-Text Consistency Checker (tools/checker)
streamlit>=1.59.2

# Optional: 8-bit / 4-bit quantization for large captioning & fusion models
# bitsandbytes>=0.49
